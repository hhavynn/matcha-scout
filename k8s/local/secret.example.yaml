apiVersion: v1
kind: Secret
metadata:
  name: matcha-scout-local-secrets
  namespace: matcha-scout-local
  labels:
    app.kubernetes.io/name: matcha-scout
type: Opaque
stringData:
  # Local Kubernetes uses USE_MOCK_AI=true, so no real Gemini key is needed.
  # Keep this placeholder only. Never commit real API keys.
  GEMINI_API_KEY: ""
